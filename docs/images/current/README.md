# Current images

Only newly generated, visually reviewed renders of the current board revision belong here.

The folder is intentionally empty in the initial repository snapshot because the current FabDesk routing pass is still running. Fresh renders will be added after the routed board passes its verification gates.
